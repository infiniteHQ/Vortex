#include <cstdint>
#include <cstddef> // Inclure l'en-tête pour avoir accès à size_t


#ifndef ICONS_
#define ICONS_

struct icons {
    // Project Icons
    static const uint8_t i_gpos[];
    static const size_t i_gpos_size;

    static const uint8_t i_rtos[];
    static const size_t i_rtos_size;

    static const uint8_t i_eapp[];
    static const size_t i_eapp_size;
    
    // Vortex Icons
    static const uint8_t i_toolchain[];
    static const size_t i_toolchain_size;

    static const uint8_t i_host[];
    static const size_t i_host_size;

    static const uint8_t i_system[];
    static const size_t i_system_size;

    static const uint8_t i_package[];
    static const size_t i_package_size;

    static const uint8_t i_script[];
    static const size_t i_script_size;

    static const uint8_t i_package_patch[];
    static const size_t i_package_patch_size;

    static const uint8_t i_package_action[];
    static const size_t i_package_action_size;

    static const uint8_t i_chroot_profile[];
    static const size_t i_chroot_profile_size;

    static const uint8_t i_library[];
    static const size_t i_library_size;

    // Basic Icons
    static const uint8_t i_refresh[];
    static const size_t i_refresh_size;

    static const uint8_t i_eye[];
    static const size_t i_eye_size;

    static const uint8_t i_bug[];
    static const size_t i_bug_size;

    static const uint8_t i_pannel[];
    static const size_t i_pannel_size;

    static const uint8_t i_tool[];
    static const size_t i_tool_size;

    static const uint8_t i_blocks[];
    static const size_t i_blocks_size;

    static const uint8_t i_terminal[];
    static const size_t i_terminal_size;

    static const uint8_t i_logs[];
    static const size_t i_logs_size;

    static const uint8_t i_pen[];
    static const size_t i_pen_size;

    static const uint8_t i_little_folder[];
    static const size_t i_little_folder_size;

    static const uint8_t i_trash[];
    static const size_t i_trash_size;

    static const uint8_t i_database[];
    static const size_t i_database_size;

    static const uint8_t i_list[];
    static const size_t i_list_size;

    static const uint8_t i_save[];
    static const size_t i_save_size;

    static const uint8_t i_add[];
    static const size_t i_add_size;

    static const uint8_t i_folder[];
    static const size_t i_folder_size;

    static const uint8_t i_settings[];
    static const size_t i_settings_size;
    
    static const uint8_t i_file[];
    static const size_t i_file_size;

    static const uint8_t i_FolderIcon[];
    static const size_t i_FolderIcon_size;
};


#endif // ICONS_